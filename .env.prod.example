# Public URL of your site (use HTTPS + your domain in production)
NEXTAUTH_URL=https://your-domain.example

# Required by next-auth in production. Generate a strong random value:
#   openssl rand -base64 32
# or
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=replace-with-strong-random-secret

# Optional: initial admin user for seeding (idempotent)
# Seed runs on each boot; it only creates the user/project if missing.
# You can remove these after primul deploy, dacă nu mai ai nevoie de ele.
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMe12345
SEED_ADMIN_NAME=Admin
