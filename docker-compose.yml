services:
  app:
    build:
      context: ./nuxt
      dockerfile: Dockerfile.nuxt
    image: client-portal:latest
    expose:
      - "3000"
    restart: unless-stopped
  caddy:
    image: caddy:2-alpine
    depends_on:
      - app
    ports:
      - "8007:80"
    volumes:
      - ./caddy:/etc/caddy
    restart: unless-stopped
